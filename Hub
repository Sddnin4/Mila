-- Mila UI Script for Roblox - New Design
-- Tạo bởi: Mila
-- Tính năng: Speed, Jump, Fly, Noclip, Teleport, Infinite Jump với Tab System

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

-- Variables
local noclip = false
local infiniteJump = false
local originalWalkSpeed = 16
local originalJumpPower = 50
local jumpConnection
local currentTab = "Home"

-- Progress Bar Function
local function showProgressBar()
    local progressGui = Instance.new("ScreenGui")
    progressGui.Name = "MilaProgress"
    progressGui.Parent = CoreGui
    
    local background = Instance.new("Frame")
    background.Size = UDim2.new(1, 0, 1, 0)
    background.Position = UDim2.new(0, 0, 0, 0)
    background.BackgroundColor3 = Color3.new(0, 0, 0)
    background.BackgroundTransparency = 0.5
    background.Parent = progressGui
    
    local progressFrame = Instance.new("Frame")
    progressFrame.Size = UDim2.new(0, 300, 0, 100)
    progressFrame.Position = UDim2.new(0.5, -150, 0.5, -50)
    progressFrame.BackgroundColor3 = Color3.fromRGB(239, 68, 109) -- Pink from image
    progressFrame.BorderSizePixel = 0
    progressFrame.Parent = progressGui
    
    local progressCorner = Instance.new("UICorner")
    progressCorner.CornerRadius = UDim.new(0, 15)
    progressCorner.Parent = progressFrame
    
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, 0, 0, 30)
    titleLabel.Position = UDim2.new(0, 0, 0, 10)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = "💖 Đang tải Mila ..."
    titleLabel.TextColor3 = Color3.new(1, 1, 1)
    titleLabel.TextSize = 16
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.Parent = progressFrame
    
    local progressBar = Instance.new("Frame")
    progressBar.Size = UDim2.new(0.8, 0, 0, 8)
    progressBar.Position = UDim2.new(0.1, 0, 0, 50)
    progressBar.BackgroundColor3 = Color3.new(1, 1, 1)
    progressBar.BackgroundTransparency = 0.3
    progressBar.Parent = progressFrame
    
    local progressBarCorner = Instance.new("UICorner")
    progressBarCorner.CornerRadius = UDim.new(0, 4)
    progressBarCorner.Parent = progressBar
    
    local progressFill = Instance.new("Frame")
    progressFill.Size = UDim2.new(0, 0, 1, 0)
    progressFill.Position = UDim2.new(0, 0, 0, 0)
    progressFill.BackgroundColor3 = Color3.new(1, 1, 1)
    progressFill.BorderSizePixel = 0
    progressFill.Parent = progressBar
    
    local fillCorner = Instance.new("UICorner")
    fillCorner.CornerRadius = UDim.new(0, 4)
    fillCorner.Parent = progressFill
    
    local statusLabel = Instance.new("TextLabel")
    statusLabel.Size = UDim2.new(1, 0, 0, 20)
    statusLabel.Position = UDim2.new(0, 0, 0, 70)
    statusLabel.BackgroundTransparency = 1
    statusLabel.Text = "Khởi tạo..."
    statusLabel.TextColor3 = Color3.new(1, 1, 1)
    statusLabel.TextSize = 12
    statusLabel.Font = Enum.Font.Gotham
    statusLabel.Parent = progressFrame
    
    local steps = {
        "Khởi tạo...",
        "Tải giao diện...",
        "Thiết lập chức năng...",
        "Hoàn thành!"
    }
    
    for i = 1, 4 do
        wait(0.5)
        statusLabel.Text = steps[i]
        local tween = TweenService:Create(progressFill, 
            TweenInfo.new(0.3, Enum.EasingStyle.Quad), 
            {Size = UDim2.new(i/4, 0, 1, 0)}
        )
        tween:Play()
    end
    
    wait(0.5)
    progressGui:Destroy()
end

-- Show progress bar first
showProgressBar()

-- UI Creation
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "MilaUI"
screenGui.Parent = CoreGui
screenGui.ResetOnSpawn = false

-- Main Frame (smaller size)
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 280, 0, 200)
mainFrame.Position = UDim2.new(0.5, -140, 0.5, -100)
mainFrame.BackgroundColor3 = Color3.new(0.9, 0.9, 0.9) -- Light gray
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = mainFrame

-- Tab System
local tabFrame = Instance.new("Frame")
tabFrame.Size = UDim2.new(1, 0, 0, 35)
tabFrame.Position = UDim2.new(0, 0, 0, 0)
tabFrame.BackgroundTransparency = 1
tabFrame.Parent = mainFrame

-- Home Tab
local homeTab = Instance.new("TextButton")
homeTab.Size = UDim2.new(0.5, -1, 1, 0)
homeTab.Position = UDim2.new(0, 0, 0, 0)
homeTab.BackgroundColor3 = Color3.new(1, 1, 1) -- Active tab
homeTab.Text = "Home"
homeTab.TextColor3 = Color3.new(0, 0, 0)
homeTab.TextSize = 14
homeTab.Font = Enum.Font.GothamBold
homeTab.Parent = tabFrame

local homeTabCorner = Instance.new("UICorner")
homeTabCorner.CornerRadius = UDim.new(0, 10)
homeTabCorner.Parent = homeTab

-- Script Tab
local scriptTab = Instance.new("TextButton")
scriptTab.Size = UDim2.new(0.5, -1, 1, 0)
scriptTab.Position = UDim2.new(0.5, 1, 0, 0)
scriptTab.BackgroundColor3 = Color3.new(0.8, 0.8, 0.8) -- Inactive tab
scriptTab.Text = "Script"
scriptTab.TextColor3 = Color3.new(0, 0, 0)
scriptTab.TextSize = 14
scriptTab.Font = Enum.Font.Gotham
scriptTab.Parent = tabFrame

local scriptTabCorner = Instance.new("UICorner")
scriptTabCorner.CornerRadius = UDim.new(0, 10)
scriptTabCorner.Parent = scriptTab

-- Content Area
local contentArea = Instance.new("Frame")
contentArea.Size = UDim2.new(1, -10, 1, -45)
contentArea.Position = UDim2.new(0, 5, 0, 40)
contentArea.BackgroundColor3 = Color3.new(1, 1, 1)
contentArea.BorderSizePixel = 0
contentArea.Parent = mainFrame

local contentCorner = Instance.new("UICorner")
contentCorner.CornerRadius = UDim.new(0, 8)
contentCorner.Parent = contentArea

-- Home Content
local homeContent = Instance.new("ScrollingFrame")
homeContent.Name = "HomeContent"
homeContent.Size = UDim2.new(1, -10, 1, -10)
homeContent.Position = UDim2.new(0, 5, 0, 5)
homeContent.BackgroundTransparency = 1
homeContent.ScrollBarThickness = 4
homeContent.CanvasSize = UDim2.new(0, 0, 0, 200)
homeContent.Parent = contentArea

-- Script Content
local scriptContent = Instance.new("ScrollingFrame")
scriptContent.Name = "ScriptContent"
scriptContent.Size = UDim2.new(1, -10, 1, -10)
scriptContent.Position = UDim2.new(0, 5, 0, 5)
scriptContent.BackgroundTransparency = 1
scriptContent.ScrollBarThickness = 4
scriptContent.CanvasSize = UDim2.new(0, 0, 0, 100)
scriptContent.Visible = false
scriptContent.Parent = contentArea

-- Player List Frame (smaller)
local playerFrame = Instance.new("Frame")
playerFrame.Name = "PlayerFrame"
playerFrame.Size = UDim2.new(0, 150, 0, 200)
playerFrame.Position = UDim2.new(0.5, 145, 0.5, -100)
playerFrame.BackgroundColor3 = Color3.new(0.9, 0.9, 0.9)
playerFrame.BorderSizePixel = 0
playerFrame.Visible = false
playerFrame.Parent = screenGui

local playerFrameCorner = Instance.new("UICorner")
playerFrameCorner.CornerRadius = UDim.new(0, 12)
playerFrameCorner.Parent = playerFrame

-- Player Title
local playerTitle = Instance.new("TextLabel")
playerTitle.Size = UDim2.new(1, 0, 0, 25)
playerTitle.Position = UDim2.new(0, 0, 0, 5)
playerTitle.BackgroundTransparency = 1
playerTitle.Text = "👥 Người Chơi"
playerTitle.TextColor3 = Color3.fromRGB(239, 68, 109)
playerTitle.TextSize = 14
playerTitle.Font = Enum.Font.GothamBold
playerTitle.Parent = playerFrame

local playerList = Instance.new("ScrollingFrame")
playerList.Name = "PlayerList"
playerList.Size = UDim2.new(1, -10, 1, -35)
playerList.Position = UDim2.new(0, 5, 0, 30)
playerList.BackgroundColor3 = Color3.new(1, 1, 1)
playerList.ScrollBarThickness = 4
playerList.Parent = playerFrame

local listCorner = Instance.new("UICorner")
listCorner.CornerRadius = UDim.new(0, 8)
listCorner.Parent = playerList

local listLayout = Instance.new("UIListLayout")
listLayout.Padding = UDim.new(0, 2)
listLayout.Parent = playerList

-- Toggle Button (when closed)
local toggleButton = Instance.new("TextButton")
toggleButton.Name = "ToggleButton"
toggleButton.Size = UDim2.new(0, 60, 0, 25)
toggleButton.Position = UDim2.new(0, 10, 0, 10)
toggleButton.BackgroundColor3 = Color3.fromRGB(239, 68, 109)
toggleButton.Text = "Mila 💖"
toggleButton.TextColor3 = Color3.new(1, 1, 1)
toggleButton.TextSize = 10
toggleButton.Font = Enum.Font.Gotham
toggleButton.Visible = false
toggleButton.Parent = screenGui

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 8)
toggleCorner.Parent = toggleButton

-- Close Button
local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"
closeButton.Size = UDim2.new(0, 20, 0, 20)
closeButton.Position = UDim2.new(1, -25, 0, 5)
closeButton.BackgroundColor3 = Color3.fromRGB(239, 68, 109)
closeButton.Text = "✕"
closeButton.TextColor3 = Color3.new(1, 1, 1)
closeButton.TextSize = 12
closeButton.Font = Enum.Font.GothamBold
closeButton.Parent = mainFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 10)
closeCorner.Parent = closeButton

-- Button Creation Function
local function createButton(name, text, position, callback, parent)
    local button = Instance.new("TextButton")
    button.Name = name
    button.Size = UDim2.new(0, 120, 0, 25)
    button.Position = position
    button.BackgroundColor3 = Color3.fromRGB(239, 68, 109)
    button.Text = text
    button.TextColor3 = Color3.new(1, 1, 1)
    button.TextSize = 11
    button.Font = Enum.Font.Gotham
    button.Parent = parent
    
    local buttonCorner = Instance.new("UICorner")
    buttonCorner.CornerRadius = UDim.new(0, 6)
    buttonCorner.Parent = button
    
    button.MouseButton1Click:Connect(callback)
    
    -- Hover effect
    button.MouseEnter:Connect(function()
        button.BackgroundColor3 = Color3.fromRGB(220, 50, 90)
    end)
    
    button.MouseLeave:Connect(function()
        button.BackgroundColor3 = Color3.fromRGB(239, 68, 109)
    end)
    
    return button
end

-- Speed/Jump Controls (smaller)
local speedLabel = Instance.new("TextLabel")
speedLabel.Size = UDim2.new(1, 0, 0, 15)
speedLabel.Position = UDim2.new(0, 0, 0, 70)
speedLabel.BackgroundTransparency = 1
speedLabel.Text = "Speed: 16"
speedLabel.TextColor3 = Color3.new(0.3, 0.3, 0.3)
speedLabel.TextSize = 10
speedLabel.Font = Enum.Font.Gotham
speedLabel.Parent = homeContent

local speedSlider = Instance.new("TextBox")
speedSlider.Size = UDim2.new(0, 60, 0, 20)
speedSlider.Position = UDim2.new(0, 5, 0, 85)
speedSlider.BackgroundColor3 = Color3.new(0.95, 0.95, 0.95)
speedSlider.Text = "16"
speedSlider.TextColor3 = Color3.new(0, 0, 0)
speedSlider.TextSize = 10
speedSlider.Parent = homeContent

local sliderCorner = Instance.new("UICorner")
sliderCorner.CornerRadius = UDim.new(0, 4)
sliderCorner.Parent = speedSlider

local jumpLabel = Instance.new("TextLabel")
jumpLabel.Size = UDim2.new(1, 0, 0, 15)
jumpLabel.Position = UDim2.new(0, 0, 0, 110)
jumpLabel.BackgroundTransparency = 1
jumpLabel.Text = "Jump: 50"
jumpLabel.TextColor3 = Color3.new(0.3, 0.3, 0.3)
jumpLabel.TextSize = 10
jumpLabel.Font = Enum.Font.Gotham
jumpLabel.Parent = homeContent

local jumpSlider = Instance.new("TextBox")
jumpSlider.Size = UDim2.new(0, 60, 0, 20)
jumpSlider.Position = UDim2.new(0, 5, 0, 125)
jumpSlider.BackgroundColor3 = Color3.new(0.95, 0.95, 0.95)
jumpSlider.Text = "50"
jumpSlider.TextColor3 = Color3.new(0, 0, 0)
jumpSlider.TextSize = 10
jumpSlider.Parent = homeContent

local jumpCorner = Instance.new("UICorner")
jumpCorner.CornerRadius = UDim.new(0, 4)
jumpCorner.Parent = jumpSlider

-- Functions
local function updatePlayerList()
    for _, child in pairs(playerList:GetChildren()) do
        if child:IsA("Frame") then
            child:Destroy()
        end
    end
    
    for _, targetPlayer in pairs(Players:GetPlayers()) do
        if targetPlayer ~= player then
            local playerContainer = Instance.new("Frame")
            playerContainer.Size = UDim2.new(1, 0, 0, 45)
            playerContainer.BackgroundColor3 = Color3.new(0.95, 0.95, 0.95)
            playerContainer.Parent = playerList
            
            local containerCorner = Instance.new("UICorner")
            containerCorner.CornerRadius = UDim.new(0, 4)
            containerCorner.Parent = playerContainer
            
            local playerName = Instance.new("TextLabel")
            playerName.Size = UDim2.new(1, 0, 0, 15)
            playerName.Position = UDim2.new(0, 0, 0, 2)
            playerName.BackgroundTransparency = 1
            playerName.Text = targetPlayer.Name
            playerName.TextSize = 10
            playerName.Font = Enum.Font.GothamBold
            playerName.TextColor3 = Color3.new(0.2, 0.2, 0.2)
            playerName.Parent = playerContainer
            
            local teleportBtn = Instance.new("TextButton")
            teleportBtn.Size = UDim2.new(0.48, 0, 0, 20)
            teleportBtn.Position = UDim2.new(0, 2, 0, 20)
            teleportBtn.BackgroundColor3 = Color3.fromRGB(239, 68, 109)
            teleportBtn.Text = "📍"
            teleportBtn.TextSize = 10
            teleportBtn.TextColor3 = Color3.new(1, 1, 1)
            teleportBtn.Parent = playerContainer
            
            local tpCorner = Instance.new("UICorner")
            tpCorner.CornerRadius = UDim.new(0, 4)
            tpCorner.Parent = teleportBtn
            
            local viewBtn = Instance.new("TextButton")
            viewBtn.Size = UDim2.new(0.48, 0, 0, 20)
            viewBtn.Position = UDim2.new(0.52, 0, 0, 20)
            viewBtn.BackgroundColor3 = Color3.fromRGB(239, 68, 109)
            viewBtn.Text = "👁️"
            viewBtn.TextSize = 10
            viewBtn.TextColor3 = Color3.new(1, 1, 1)
            viewBtn.Parent = playerContainer
            
            local viewCorner = Instance.new("UICorner")
            viewCorner.CornerRadius = UDim.new(0, 4)
            viewCorner.Parent = viewBtn
            
            teleportBtn.MouseButton1Click:Connect(function()
                if targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    character.HumanoidRootPart.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame
                end
            end)
            
            viewBtn.MouseButton1Click:Connect(function()
                if targetPlayer.Character and targetPlayer.Character:FindFirstChild("Humanoid") then
                    workspace.CurrentCamera.CameraSubject = targetPlayer.Character.Humanoid
                end
            end)
        end
    end
    
    playerList.CanvasSize = UDim2.new(0, 0, 0, #Players:GetPlayers() * 47)
end

local function toggleNoclip()
    noclip = not noclip
    if noclip then
        local connection
        connection = RunService.Stepped:Connect(function()
            if not noclip then
                connection:Disconnect()
                return
            end
            for _, part in pairs(character:GetChildren()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end)
    else
        for _, part in pairs(character:GetChildren()) do
            if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                part.CanCollide = true
            end
        end
    end
end

local function toggleInfiniteJump()
    infiniteJump = not infiniteJump
    if infiniteJump then
        jumpConnection = UserInputService.JumpRequest:Connect(function()
            if character and character:FindFirstChild("Humanoid") then
                character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end)
    else
        if jumpConnection then
            jumpConnection:Disconnect()
            jumpConnection = nil
        end
    end
end

local function resetCamera()
    workspace.CurrentCamera.CameraSubject = character.Humanoid
end

local function loadFlyScript()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/GhostPlayer352/Test4/main/Vehicle%20Fly%20Gui"))()
    end)
end

-- Tab System Functions
local function switchTab(tabName)
    currentTab = tabName
    if tabName == "Home" then
        homeContent.Visible = true
        scriptContent.Visible = false
        homeTab.BackgroundColor3 = Color3.new(1, 1, 1)
        scriptTab.BackgroundColor3 = Color3.new(0.8, 0.8, 0.8)
        homeTab.Font = Enum.Font.GothamBold
        scriptTab.Font = Enum.Font.Gotham
    else
        homeContent.Visible = false
        scriptContent.Visible = true
        homeTab.BackgroundColor3 = Color3.new(0.8, 0.8, 0.8)
        scriptTab.BackgroundColor3 = Color3.new(1, 1, 1)
        homeTab.Font = Enum.Font.Gotham
        scriptTab.Font = Enum.Font.GothamBold
    end
end

-- Create Home Buttons (2x3 layout, smaller)
createButton("SpeedButton", "🏃 Speed", UDim2.new(0, 5, 0, 5), function()
    local speed = tonumber(speedSlider.Text) or 16
    humanoid.WalkSpeed = speed
    speedLabel.Text = "Speed: " .. speed
end, homeContent)

createButton("JumpButton", "🦘 Jump", UDim2.new(0, 135, 0, 5), function()
    local jump = tonumber(jumpSlider.Text) or 50
    humanoid.JumpPower = jump
    jumpLabel.Text = "Jump: " .. jump
end, homeContent)

createButton("NoclipButton", "👻 Noclip", UDim2.new(0, 5, 0, 35), function()
    toggleNoclip()
end, homeContent)

createButton("InfiniteJumpButton", "♾️ InfJump", UDim2.new(0, 135, 0, 35), function()
    toggleInfiniteJump()
end, homeContent)

createButton("TeleportButton", "📍 Players", UDim2.new(0, 5, 0, 150), function()
    playerFrame.Visible = not playerFrame.Visible
    if playerFrame.Visible then
        updatePlayerList()
    end
end, homeContent)

createButton("CameraButton", "📷 Camera", UDim2.new(0, 135, 0, 150), function()
    resetCamera()
end, homeContent)

-- Create Script Tab Content
local scriptLabel = Instance.new("TextLabel")
scriptLabel.Size = UDim2.new(1, 0, 0, 20)
scriptLabel.Position = UDim2.new(0, 0, 0, 5)
scriptLabel.BackgroundTransparency = 1
scriptLabel.Text = "🚀 Script Hub"
scriptLabel.TextColor3 = Color3.fromRGB(239, 68, 109)
scriptLabel.TextSize = 12
scriptLabel.Font = Enum.Font.GothamBold
scriptLabel.Parent = scriptContent

createButton("FlyButton", "🕊️ Vehicle Fly", UDim2.new(0, 5, 0, 30), function()
    loadFlyScript()
end, scriptContent)

-- Tab Click Events
homeTab.MouseButton1Click:Connect(function()
    switchTab("Home")
end)

scriptTab.MouseButton1Click:Connect(function()
    switchTab("Script")
end)

-- Close/Toggle functionality
closeButton.MouseButton1Click:Connect(function()
    mainFrame.Visible = false
    toggleButton.Visible = true
    playerFrame.Visible = false
end)

toggleButton.MouseButton1Click:Connect(function()
    mainFrame.Visible = true
    toggleButton.Visible = false
end)

-- Keyboard shortcut
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.RightShift then
        if mainFrame.Visible then
            mainFrame.Visible = false
            toggleButton.Visible = true
            playerFrame.Visible = false
        else
            mainFrame.Visible = true
            toggleButton.Visible = false
        end
    end
end)

-- Character respawn handling
player.CharacterAdded:Connect(function(newCharacter)
    character = newCharacter
    humanoid = character:WaitForChild("Humanoid")
    noclip = false
    infiniteJump = false
    if jumpConnection then
        jumpConnection:Disconnect()
        jumpConnection = nil
    end
    wait(1)
    humanoid.WalkSpeed = originalWalkSpeed
    humanoid.JumpPower = originalJumpPower
end)

print("Mila UI đã được tải thành công! 💖")
print("Nhấn Right Shift để bật/tắt UI")